import { AuthChildrenGuard.Ts } from './auth-children-guard.ts';

describe('AuthChildrenGuard.Ts', () => {
  it('should create an instance', () => {
    expect(new AuthChildrenGuard.Ts()).toBeTruthy();
  });
});
